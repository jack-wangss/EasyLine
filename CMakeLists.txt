cmake_minimum_required(VERSION 3.15)

project(EasyLineProject LANGUAGES C CXX)

# Add the EasyLine subdirectory which contains the main project CMakeLists
add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/EasyLine)
